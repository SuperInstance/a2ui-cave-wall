[package]
name = "a2ui-cave-wall"
version = "0.1.0"
edition = "2021"
description = "The cave wall translation layer — where agent text projections become human-readable"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
